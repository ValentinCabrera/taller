import { render, screen } from '@testing-library/react';
import DetailOrden from './DetailOrden';

test('renders DetailOrden component', () => {
    render(<DetailOrden />);
    // Add your assertions here to test the component's behavior
});